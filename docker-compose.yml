name: portfolio-sample-api

services:
  flask-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: portfolio-api-sample:1.0
    ports:
      - "5000:5000"
    volumes:
      - .:/api
    environment:
      FLASK_ENV: development
      FLASK_DEBUG: "1"
    networks:
      - portfolio-api-net
    depends_on:
      - redis

  redis:
    image: redis:8
    # Uncomment the following lines to container for testing purposes
    # ports:
    #   - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - portfolio-api-net

networks:
  portfolio-api-net:
    driver: bridge

volumes:
  redis-data: